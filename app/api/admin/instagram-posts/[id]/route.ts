import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { createAdminClient } from '@/lib/supabase/admin'
import { isAdminUser } from '@/lib/supabase/admin-auth'
async function authorized(){const{data:{user}}=await(await createClient()).auth.getUser();return isAdminUser(user)}
function getPostUrl(embedCode:string,fallback:string){const m=embedCode?.match(/data-instgrm-permalink=["']([^"']+)["']/i);return m?.[1]||fallback||''}
export async function PUT(request:Request,{params}:{params:Promise<{id:string}>}){if(!(await authorized()))return NextResponse.json({error:'Unauthorized'},{status:401});const{id}=await params;const b=await request.json();const embed_code=String(b.embed_code||'').trim();const post_url=getPostUrl(embed_code,String(b.post_url||'').trim());if(!embed_code||!post_url)return NextResponse.json({error:'Paste the Instagram embed code. A post URL is extracted from it.'},{status:400});if(!/<blockquote[^>]*class=["'][^"']*instagram-media/i.test(embed_code))return NextResponse.json({error:'That does not look like an Instagram embed block.'},{status:400});const{data,error}=await createAdminClient().from('instagram_posts').update({image_url:String(b.image_url||''),post_url,embed_code,caption:String(b.caption||''),status:b.status==='published'?'published':'draft',sort_order:Number(b.sort_order||0),updated_at:new Date().toISOString()}).eq('id',id).select().single();if(error)return NextResponse.json({error:'Failed to update Instagram post'},{status:500});return NextResponse.json({success:true,data})}
export async function DELETE(_request:Request,{params}:{params:Promise<{id:string}>}){if(!(await authorized()))return NextResponse.json({error:'Unauthorized'},{status:401});const{id}=await params;const{error}=await createAdminClient().from('instagram_posts').delete().eq('id',id);if(error)return NextResponse.json({error:'Failed to delete Instagram post'},{status:500});return NextResponse.json({success:true})}
