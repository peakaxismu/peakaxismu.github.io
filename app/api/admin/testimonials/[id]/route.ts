import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { createAdminClient } from '@/lib/supabase/admin'
import { isAdminUser } from '@/lib/supabase/admin-auth'
async function authorized(){const s=await createClient();const{data:{user}}=await s.auth.getUser();return isAdminUser(user)}
export async function PUT(request:Request,{params}:{params:Promise<{id:string}>}){if(!(await authorized()))return NextResponse.json({error:'Unauthorized'},{status:401});const b=await request.json();if(!b.quote||!b.name||!b.activity)return NextResponse.json({error:'Quote, name and activity are required'},{status:400});if(b.status&&!['draft','published'].includes(b.status))return NextResponse.json({error:'Invalid status'},{status:400});const{data,error}=await createAdminClient().from('testimonials').update({quote:b.quote,name:b.name,activity:b.activity,status:b.status||'published',sort_order:Number(b.sort_order)||0,updated_at:new Date().toISOString()}).eq('id',(await params).id).select().single();if(error)return NextResponse.json({error:'Failed to update testimonial'},{status:500});return NextResponse.json({success:true,data})}
export async function DELETE(_request:Request,{params}:{params:Promise<{id:string}>}){if(!(await authorized()))return NextResponse.json({error:'Unauthorized'},{status:401});const{error}=await createAdminClient().from('testimonials').delete().eq('id',(await params).id);if(error)return NextResponse.json({error:'Failed to delete testimonial'},{status:500});return NextResponse.json({success:true})}
